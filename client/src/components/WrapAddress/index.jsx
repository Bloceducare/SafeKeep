import Styled from "styled-components";

const WrapAddress = Styled.span`
  overflow-wrap: break-word;
  word-wrap: break-word;
  hyphens: auto;
  font-size: 0.95rem;

`;

export default WrapAddress;
